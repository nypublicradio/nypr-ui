
import { string } from 'dummy/helpers/string';
import { module, test } from 'qunit';

module('Unit | Helper | string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = string([42]);
  assert.ok(result);
});

