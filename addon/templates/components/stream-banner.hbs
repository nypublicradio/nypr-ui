<div class="wqxr-streambanner-card"
     style="background-image:url('/assets/img/backgrounds/streambanner.jpg')">
  <div>
    {{#if streams.isFulfilled}}
    <div class="wqxr-streambanner-station nudge">
      <span class="wqxr-streambanner-on-air">On air now on </span>
      {{#power-select
        renderInPlace=true
        searchEnabled=false
        dropdownClass="wqxr-streambanner-dropdown"
        selected=activeStream
        options=streams
        onchange=(action (mut activeStream))
        as |stream|}}
        {{stream.name}}
      {{/power-select}}
    </div>
    <div class="wqxr-streambanner-details">
      <h3 class="wqxr-streambanner-title">
        {{yield activeStream}}
      </h3>
    </div>
    <div class="wqxr-streambanner-controls">
      {{#listen-button
        audio=audio
        class='wqxr-streambanner-listenbutton nudge'
        type='blue-boss'
        state=(if (eq activeStream.slug audio.currentAudio.id) audio.playState)
        itemPK=activeStream.slug
        itemTitle=activeStream.name}}
        Listen Live
      {{/listen-button}}
    </div>
    {{else}}
    Loading...
    {{/if}}
  </div>
</div>
