{{#if hasBlock}}
  {{yield (hash
    title=(component 'nypr-card/title' text=title)
    button=(component 'nypr-card/button')
  )}}
{{else}}
  {{nypr-card/title text=title}}
  {{#if buttonAction}}
    {{nypr-card/button click=(action buttonAction) text=buttonText}}
  {{/if}}
{{/if}}
