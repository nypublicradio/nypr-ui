{{#if active}}
  {{#nypr-card as |card|}}
    {{yield (hash
      button=(component 'nypr-card/button' click=(action 'goTo' to))
      header=(component (get card 'header') title=title)
    )}}
  {{/nypr-card}}
{{/if}}
