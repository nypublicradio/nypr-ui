{{#if hasBlock}}
  {{yield}}
{{else}}
  {{#listen-button
    audio=audio
    class='stream-banner-listenbutton nudge'
    type='blue-boss'
    state=(if (eq activeStream.slug audio.currentId) audio.playState)
    itemPK=activeStream.slug
    itemTitle=activeStream.name}}
    Listen Live
  {{/listen-button}}
{{/if}}
