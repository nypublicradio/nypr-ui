{{#if hasBlock}}
  {{yield (hash
    header=(component 'nypr-card/header')
    alert=(component 'nypr-card/alert')
  )}}
{{else}}
  {{nypr-card/header
    title=title
    buttonAction=buttonAction}}

  {{component body}}
{{/if}}
