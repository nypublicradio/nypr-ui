import Component from '@ember/component';
import layout from '../../../templates/components/nypr-ui/brand-header/header-logos';

export default Component.extend({
  layout,
  classNames: ['nypr-brand-header__logo-list'],
});
