import Ember from 'ember';
import layout from '../../templates/components/nypr-story/show-sidebar-top';

export default Ember.Component.extend({
  layout,
  classNames: ['nypr-story-show-sidebar-top'],
});