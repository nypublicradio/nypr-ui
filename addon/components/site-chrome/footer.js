import Component from '@ember/component';
import layout from '../../templates/components/site-chrome/footer';

export default Component.extend({
  layout,
  tagName: 'footer',
  classNames: ['site-chrome__footer']
});
