import Component from '@ember/component';
import layout from '../../../templates/components/site-chrome/top/body';

export default Component.extend({
  layout,
  classNames: ['site-chrome__top__body']
});
