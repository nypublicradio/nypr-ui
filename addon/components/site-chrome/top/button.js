import Ember from 'ember';
import layout from '../../../templates/components/site-chrome/top/button';

export default Ember.Component.extend({
  layout,
  tagName: 'button',
  classNames: ['site-chrome__top-button']
});
