import Component from '@ember/component';
import layout from '../../../templates/components/site-chrome/nav/footer';

export default Component.extend({
  layout,
  classNames: ['site-chrome__nav__footer']
});
