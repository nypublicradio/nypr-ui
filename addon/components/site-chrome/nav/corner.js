import Ember from 'ember';
import layout from '../../../templates/components/site-chrome/nav/corner';

export default Ember.Component.extend({
  layout,
  classNames: ['site-chrome__left-corner']
});
