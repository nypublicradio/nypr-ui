import Component from '@ember/component';
import layout from '../../../templates/components/site-chrome/nav/links';

export default Component.extend({
  layout,
  classNames: ['site-chrome__links']
});
