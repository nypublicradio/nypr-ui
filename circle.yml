machine:
  node:
    version: 6.9.5

dependencies:
  pre:
    - sudo apt-get update && sudo apt-get install ruby-sass
    - gem update --system
    - gem install compass --no-ri --no-rdoc
    - npm i bower -g
    - bower install
    - npm install
